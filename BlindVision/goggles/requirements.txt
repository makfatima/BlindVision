# Smart Goggles (Raspberry Pi 4B, Raspberry Pi OS Bookworm 64-bit)
# On-device YOLOv8n inference, CPU-only, no hardware accelerator (Table XIX).

ultralytics==8.2.28
torch>=2.2,<2.4
opencv-python-headless>=4.9
numpy==1.26.4
bleak>=0.22          # BLE 5.0 client (stick link)
pyttsx3==2.90         # offline-preferred TTS
requests>=2.31        # caregiver/backend relay over TLS 1.2
pyyaml>=6.0
pyserial>=3.5         # GPS (NEO-6M) / GSM (SIM800L) AT-command serial I/O

# Dev / test
pytest>=8.0
